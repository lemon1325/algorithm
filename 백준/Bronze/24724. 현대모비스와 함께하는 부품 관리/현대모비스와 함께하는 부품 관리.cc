#include<iostream>
using namespace std;

int main() {
	int t;
	cin >> t;
	for (int i = 1; i <= t; i++) {
		cout << "Material Management " << i << endl;
		int n, a, b; //a는 크기 제한, b는 무게 제한
		cin >> n >> a >> b;
		for (int j = 0; j < n; j++) {
			int u,v;
			cin >> u >> v;

		}
		cout << "Classification ---- End!" << endl;
	}

	return 0;
}